name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Use v
        uses: nzbr/vlang-action@v1
      - name: fmt and test
        run: |
          v test-fmt
          v -stats test ./
        env:
          CI: true
